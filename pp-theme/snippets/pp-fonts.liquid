{%- comment -%}
  Tipografía compartida. Princess Polly usa Forma DJR Banner, de licencia cerrada;
  Archivo es la grotesca de Google Fonts con proporciones y altura de x más
  parecidas, así que el conjunto mantiene el mismo color de página.

  Cada sección lo renderiza por su cuenta para no depender del layout: `render`
  aísla el ámbito y no permite un guardia compartido, pero el navegador resuelve
  el mismo href una sola vez.
{%- endcomment -%}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Archivo:wght@400;500;600;700;800&display=swap" rel="stylesheet">
