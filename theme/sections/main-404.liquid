<div class="k-wrap">
  <div class="k-empty">
    <h1>Página no encontrada</h1>
    <p style="color:var(--k-muted);margin-bottom:22px">El enlace que seguiste no existe o fue movido.</p>
    <a class="k-btn k-btn--primary" href="{{ routes.root_url }}">Volver al inicio</a>
  </div>
</div>

{% schema %}
{ "name": "404", "tag": "section", "settings": [] }
{% endschema %}
